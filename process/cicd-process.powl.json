{
  "type": "choice_graph",
  "name": "cargo-cicd manufacturing process",
  "version": "v26.6.2",
  "description": "Lawful command ordering for cargo-cicd CI/CD manufacturing",
  "activities": [
    "status:show", "status:audit",
    "target:show", "target:prune",
    "test:changed", "trybuild:changed",
    "workspace:doctor",
    "publish:run",
    "evidence:audit", "receipt:write"
  ],
  "partial_order": [
    { "from": "status:show", "to": "test:changed", "type": "required_predecessor" },
    { "from": "test:changed", "to": "publish:run", "type": "required_predecessor" },
    { "from": "status:audit", "to": "receipt:write", "type": "required_predecessor" }
  ],
  "required_stages": ["status:show", "status:audit"],
  "object_types": {
    "ProcessEvidence": {
      "created_by": ["status:show", "test:changed", "publish:run"],
      "terminated_by": ["evidence:audit"],
      "schema": "schemas/receipts/process-evidence.json",
      "min_count": 1
    }
  },
  "trace_classes": {
    "pipeline_run": {
      "description": "Complete sequential execution of all declared activities",
      "target_verdict": "TRUTHFUL",
      "required_activities": [
        "status:show", "target:show", "test:changed", "trybuild:changed",
        "workspace:doctor", "publish:run", "status:audit", "evidence:audit", "receipt:write"
      ]
    },
    "live_workspace": {
      "description": "Accumulated ambient command history from individual invocations",
      "target_verdict": "VARIANCE",
      "note": "VARIANCE is expected and honest for live_workspace traces"
    }
  },
  "admission_gate": {
    "type": "conformance",
    "required_score": 0.95,
    "adjudicator": "wasm4pm",
    "command": "wpm audit audit-events.xes",
    "note": "simd_token_replay derives DFG from trace; 3-pass canonical XES achieves 0.9636 fitness (TRUTHFUL). required_score is 0.95 (TRUTHFUL threshold). audit file is audit-events.xes (stable canonical path, not events.xes)."
  }
}
